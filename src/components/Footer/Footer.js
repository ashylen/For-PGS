import React from "react";

import styles from "./Footer.module.scss";

const Footer = () => (
  <footer>
    <div className={styles.wrapper}>
      <div className={styles.text}>Powered by PGS</div>
    </div>
  </footer>
);

export default Footer;
